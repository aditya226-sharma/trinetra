# Core runtime (always needed).
fastapi>=0.111
uvicorn[standard]>=0.30
PyYAML>=6.0
requests>=2.32

# Optional acceleration — pure-Python pipeline works without these.
networkx>=3.0          # Module C entity graph
numpy>=2.0,<3          # Module A feature vectors (optional)
scikit-learn>=1.4      # Module A trained classifier when use_sklearn=true

# Tooling.
pytest>=8.0
httpx>=0.27